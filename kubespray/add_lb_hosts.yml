---
- name: Add lb-apiserver.kubernetes.local to /etc/hosts on all nodes
  hosts: all
  become: yes

  tasks:
    - name: Add lb-apiserver entry to /etc/hosts
      lineinfile:
        path: /etc/hosts
        line: "10.10.0.9 lb-apiserver.kubernetes.local"
        state: present
